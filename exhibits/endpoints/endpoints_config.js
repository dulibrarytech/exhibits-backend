'use strict';

const APP_PATH = '/exhibits-dashboard';
const PREFIX = '/api/';
const VERSION = 'v1';
const ENDPOINT = '/exhibits';

module.exports = {
    APP_PATH,
    PREFIX,
    VERSION,
    ENDPOINT
};
